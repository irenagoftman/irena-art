# Irena Goftman — Portfolio (React + Vite + Tailwind)
Local run:
  npm install
  npm run dev

Deploy to Vercel: import repo → Deploy. Customize texts in src/App.jsx, artworks in src/data/artworks.js, images in public/artworks/.
